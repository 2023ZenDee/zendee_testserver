const { PrismaClient } = require('@prisma/client');
const prisma = new PrismaClient();

const getReportedUser = async(req,res) =>{
    try{
        const reportedUser = prisma.rep 
    }catch(err){

    }
}